# CLAUDE.md — Project Instructions

You are picking up work on a real manuscript ("Explainable AI-Based Intrusion Detection for Smart Grid Cyber-Physical Systems") that is at **Minor Revision** for Scientific Reports. Reviewer 4 gave 7 specific comments. All 7 have already been drafted into the manuscript and response letter, but three of them (Reviewer points 1, 2, 3) currently have `[RUN & INSERT]` placeholders instead of real numbers, because they require genuine new computation that hadn't been run yet. Your job is to run that computation and complete the revision.

## THE ONE RULE THAT OVERRIDES EVERYTHING ELSE

**Never invent, estimate, or plausibly-guess a number for this manuscript.** Every `[RUN & INSERT]` marker must be replaced only with output you actually generated by running real code against the real dataset/models in this repository. If a script fails, fix the script and re-run it — do not paper over the failure with a number that "looks about right." If you cannot get a real result for something (e.g. a required file doesn't exist), stop and tell the user exactly what's missing, rather than filling the gap yourself. This project has already gone through prior revision rounds specifically built around a strict non-fabrication discipline (a real train/test leakage bug was found and fixed rather than the inflated ~0.997 accuracy being kept) — do not break that discipline now. This rule applies to every number, every table cell, and every "the result was X" sentence in both `manuscript_revision3.md` and `response_to_reviewers.md`.

## Files in this repo you need to know about

- `manuscript_revision3.md` — the full manuscript, already restructured with the correct section numbering (new Sections 1.1, 3.6.1, 4.7, 4.9; renumbered 4.8, 4.10) to accommodate the reviewer-requested additions. Contains multiple `[RUN & INSERT: ...]` markers, each with a description of exactly what's needed.
- `response_to_reviewers.md` — the formal cover letter for resubmission (Parts A–E), quoting all reviewers and responding point-by-point. Contains the same numbers as `[RUN & INSERT]` markers, in Part E.
- `run_repeated_cv_and_policy_eval.py` — a ready-to-run script that: (1) loads the real dataset via `config/config.yaml`, (2) runs a repeated 5×10-fold cross-validation for statistical power (Reviewer point 2 → Table 3c), and (3) benchmarks the prevention/policy layer's response accuracy, false-block rate, and latency by re-scoring real out-of-fold predictions through the actual `src/api.py::compute_preventive_action` function (Reviewer point 1 → Section 4.9). It prints "ready-to-paste" text blocks for both.
- `src/` — the actual production codebase (api.py, model.py, preprocess.py, train.py, train_unsw.py, utils.py). This is real, working code — reuse it, don't reimplement it.
- `config/config.yaml` — real hyperparameters and feature list.
- `data/external/enhanced_realistic_smartgrid.csv` — the real synthetic training dataset (30,000 rows).

## What is NOT yet in this repo (you need to find or ask about it)

Manuscript Table 8 already reports real external-validation results on the MSU/ORNL Power System Attack Dataset (four models × three label granularities: binary, 3-class, 37-class). That specific pipeline is **not** `src/train_unsw.py` as it currently exists in this repo — that script only trains a single Random Forest on one label granularity. The actual Table 8 pipeline was built separately in an earlier revision round and may exist elsewhere in this project (a different script, a notebook, or a saved `models_unsw*/` directory with multiple trained model files). **Search the repository for it before assuming it doesn't exist.** Look for: additional Python files or notebooks referencing MSU/ORNL, "37-class" / "3-class" / "binary", or directories under `models_unsw*`. If you genuinely cannot find it, tell the user directly — do not attempt to rebuild Table 8's numbers from scratch and do not touch the existing Table 8 values (they are real, already-published-round results and are NOT placeholders).

## Your task, in order

1. **Run `run_repeated_cv_and_policy_eval.py`.** Fix any environment issues that come up (missing packages, wrong CWD, etc.) — the script is designed to run from the project root. If `catboost` isn't installed, either install it (`pip install catboost`) or let the script's graceful fallback skip it, but tell the user which happened.

2. **Take the script's real console output** and replace every matching `[RUN & INSERT]` marker in `manuscript_revision3.md` — this covers Table 3c (Section 3.6.1), the interpretive sentence in Section 3.6.1, the Abstract's repeated-CV summary sentence, the Conclusion's repeated-CV sentence, the Section 4.10 "Fifth" limitation sentence, and Section 4.9's response accuracy / false-block rate / latency numbers and sentences. Use the script's own "Ready-to-paste" text verbatim where provided — it's already worded to match the manuscript's tone.

3. **Locate the real MSU/ORNL Table-8 pipeline** (see above). If found, adapt it to call `run_msu_ornl_shap()` from `run_repeated_cv_and_policy_eval.py` (or write an equivalent short script next to it) using the SAME already-trained model and test split that produced Table 8 — do not retrain a new model just for this. Use the real output to fill Table 8b and the discussion paragraph in Section 4.7 of `manuscript_revision3.md`.
   - If you cannot find or reconstruct that pipeline, stop and report this clearly to the user instead of guessing at feature names or SHAP values. Do not leave the placeholder silently — flag it as blocked and explain what's needed to unblock it (e.g., "the MSU/ORNL model object and test set from Table 8 need to be provided").

4. **Update `response_to_reviewers.md` (Part E)** with the exact same real numbers you just inserted into the manuscript — the letter and the manuscript must never disagree with each other.

5. **Final consistency pass:**
   - Search both `manuscript_revision3.md` and `response_to_reviewers.md` for the literal string `[RUN & INSERT` — there must be zero occurrences left in either file when you're done. If any remain because of step 3's blocker, list them explicitly to the user rather than silently leaving them.
   - Check that every place a number appears in more than one location (e.g., response accuracy appears in Section 4.9, the Abstract, and the Conclusion) uses the identical value everywhere.
   - Check that section/table cross-references are still correct after your edits (e.g., if you added new figures/tables, make sure their numbers don't collide with existing ones).
   - Delete the `> **AUTHOR NOTE — DELETE BEFORE SUBMISSION.**` callout at the top of `manuscript_revision3.md` once everything above it is confirmed clean.

## Definition of done

- Both `.md` files contain zero `[RUN & INSERT]` markers (or, for the MSU/ORNL SHAP piece specifically, an explicit, clearly-flagged note to the user about what's blocking it — never a fabricated substitute).
- Every number that appears in multiple places is consistent across the manuscript and the response letter.
- You can point to the exact script run (and its console output) that produced every new number in the document — if the user asks "where did this number come from," you should be able to answer with a specific command and a specific line of output, not "I calculated it."
- The AUTHOR NOTE has been removed from the final manuscript.

## If you get stuck

Tell the user directly what's blocking you and what you need from them (a missing file, a package that won't install, an ambiguous instruction). Don't guess past a genuine blocker — on a document like this, a flagged gap is far better than a quietly wrong number.
